<% @title = "#{t("ads.new")} "%>

<h1><%= t("ads.new")%></h1>

<% form_for(@ad , :html => { :multipart => true }) do |f| %>

  <%= render :partial => 'form_fields', :locals =>{:f => f} %>
  <br/>
  <p>
    <%= f.submit 'Criar' %>
  </p>
<% end %>