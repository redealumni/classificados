<h1>Alterar Classificado</h1>

<% form_for(@ad) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form_fields', :locals =>{:f => f} %>
  
  <p>
    <%= f.submit 'Atualizar' %>
  </p>
<% end %>

<%= link_to 'Voltar', ads_path %>